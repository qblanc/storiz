// function insertCharShow(charShowHTML) {
//   const div = document.getElementById('char-show');
//   div.innerHTML = "";
//   div.innerHTML = charShowHTML;
// };

// // insertCharShow("<%= j render "characters/show", character: @character %>");

// console.log('hello world')
// function insertCharShowOnClick() {
//   // if(document.querySelectorAll('.search-scene-results')) {
//     const searchSceneResults = document.querySelectorAll('.search-scene-results')
//     console.log(searchSceneResults)

//     searchSceneResults.forEach((link) => {

//       link.addEventListener('click', (event) => {
//         console.log(link)
//         insertCharShow("<%= j render 'characters/show', character: Character.find(link.dataset.id) %>");
//       });
//     });
//   // };
// };

